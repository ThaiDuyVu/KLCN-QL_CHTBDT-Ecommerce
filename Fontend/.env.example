# Use same-origin /api via the Vite proxy. VITE_* values are public: no secrets.
VITE_API_BASE_URL=/api
BACKEND_PROXY_TARGET=http://localhost:8080
VITE_CSRF_COOKIE_NAME=XSRF-TOKEN
VITE_CSRF_HEADER_NAME=X-XSRF-TOKEN
